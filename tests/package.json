{
  "name": "@apite/sw6-user-tests",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "local": "node_modules/.bin/newman run -r cli ./postman/collection.json -e ./postman/environment.json -g ./postman/globals.json --color=on --bail",
    "postman-ci": "node_modules/.bin/newman run -r cli,htmlextra,junit ./postman/collection.json -e ./postman/environment.json -g ./postman/globals.json --color=on --insecure --no-insecure-file-read"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "newman": "latest",
    "newman-reporter-htmlextra": "latest"
  }
}
